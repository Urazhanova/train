# 🔥 Hot Reload Configuration

Feedback Trainer настроен на automatic hot reload для быстрой разработки.

## 📡 Что это?

**Hot Reload** = автоматическое обновление кода при изменении файлов:
- **Backend (Python):** UVicorn --reload автоматически перезагружает сервер
- **Frontend (React):** Fast Refresh обновляет UI без перезагрузки страницы

## 🚀 Быстрый старт (One Command)

Откройте терминал в корневой папке проекта:

```bash
cd "/Users/irinaurazanova/Desktop/Демо тренажер"
chmod +x start-dev.sh
./start-dev.sh
```

**Что произойдет:**
1. Откроются 2 новых терминала (Backend + Frontend)
2. Оба сервера запустятся с hot reload enabled
3. Браузер откроется на http://localhost:3333

---

## 🔄 Как это работает?

### Backend (uvicorn --reload)

**Файлы для отслеживания:**
- ✅ `backend/*.py` - любые Python файлы
- ✅ `backend/services/*.py` - сервисы
- ✅ `backend/.env` - конфигурация

**Процесс:**
```
Вы изменяете файл → Uvicorn обнаруживает изменение
  → Backend перезагружается (0.5-2 сек)
  → API готов к новым запросам
```

**Пример:**
```python
# Измените в main.py
assistant_response = groq_service.get_feedback_response(...)
# Сохраните → Backend автоматически перезагрузится!
```

### Frontend (React Fast Refresh)

**Файлы для отслеживания:**
- ✅ `frontend/src/**/*.tsx` - компоненты
- ✅ `frontend/src/**/*.ts` - сервисы
- ✅ `frontend/src/styles/*.css` - стили

**Процесс:**
```
Вы изменяете компонент → React обнаруживает изменение
  → Горячее обновление (instantaneous, < 100ms)
  → Состояние приложения сохраняется
  → UI обновляется без перезагрузки страницы
```

**Пример:**
```tsx
// Измените в DialogPage.tsx
const messageCount = conversationHistory.filter((m) => m.role === 'user').length;
// Сохраните → UI обновится instantly, без потери состояния!
```

---

## 📝 Ручной запуск (если нужны разные терминалы)

### Terminal 1: Backend

```bash
cd backend
source venv/bin/activate
./run-dev.sh

# или напрямую
uvicorn main:app --reload --reload-dirs . --host 0.0.0.0 --port 8888
```

### Terminal 2: Frontend

```bash
cd frontend
npm run dev

# или напрямую
PORT=3333 REACT_APP_API_URL=http://localhost:8888 npm start
```

---

## 🎯 Как проверить, что hot reload работает?

### Backend

```bash
# 1. Измените текст в main.py
"Feedback Trainer API is running" → "Feedback Trainer API is running NOW"

# 2. Посмотрите в терминал backend - увидите:
WARNING:  StatReload detected changes in 'main.py'. Reloading...
INFO:     Application startup complete.

# 3. Проверьте в браузере F12 → Network
curl http://localhost:8888/health
# Должен вернуть новое сообщение
```

### Frontend

```bash
# 1. Измените текст в DialogPage.tsx
"Напишите ваш ответ..." → "Напишите свой ответ..."

# 2. Сохраните → посмотрите в браузер
# Текст обновится instantly, без перезагрузки!

# 3. Посмотрите в терминал frontend - увидите:
webpack compiled successfully
```

---

## ⚙️ Конфигурация

### Backend: requirements.txt

```
uvicorn[standard]==0.24.0  # [standard] = с автоперезагрузкой
```

### Frontend: .env.development

```
FAST_REFRESH=true              # Включить Fast Refresh
GENERATE_SOURCEMAP=false       # Ускоренная сборка
SKIP_PREFLIGHT_CHECK=true      # Skip проверок для скорости
```

---

## 🚨 Проблемы и решения

### Проблема: Backend не перезагружается

**Решение:**
```bash
# Убедитесь, что используете --reload флаг
uvicorn main:app --reload

# Проверьте логи
# Должны быть строки с "StatReload detected changes"
```

### Проблема: Frontend не обновляется

**Решение:**
```bash
# 1. Сохраните файл (Ctrl+S)
# 2. Посмотрите в браузер - должно обновиться
# 3. Если нет - откройте DevTools (F12 → Console)
# 4. Проверьте есть ли ошибки

# Если не помогает - перезагрузите страницу (Cmd+R)
```

### Проблема: "Cannot find module"

**Решение:**
```bash
# Убедитесь, что все зависимости установлены
cd backend && pip install -r requirements.txt
cd ../frontend && npm install

# Перезагрузите IDE/терминал
```

---

## 💡 Best Practices

### Для Backend
```python
# ✅ ХОРОШО - изменения перезагружаются
def new_endpoint():
    return {"message": "updated"}

# ❌ ПЛОХО - нужна перезагрузка вручную
# Если используете глобальные переменные которые меняются на стартапе
```

### Для Frontend
```tsx
// ✅ ХОРОШО - изменения применяются instantly
const MyComponent = () => {
  const [state, setState] = useState(0);
  return <button>Count: {state}</button>;
};

// ❌ ПЛОХО - может потребоваться перезагрузка
// Если меняются type definitions или импорты
```

---

## 🔗 Важные порты

| Сервис | Порт | Адрес | Hot Reload |
|--------|------|-------|-----------|
| Backend | 8888 | http://localhost:8888 | ✅ Автоперезагрузка (uvicorn --reload) |
| Frontend | 3333 | http://localhost:3333 | ✅ Fast Refresh (React) |
| API Docs | 8888 | http://localhost:8888/docs | Автоматически |

---

## 📊 Время обновления

| Компонент | Время | Метод |
|-----------|-------|--------|
| Backend Python файлы | 0.5-2 сек | StatReload (file watching) |
| Frontend TypeScript | < 100 мс | Fast Refresh (HMR) |
| Frontend CSS | < 100 мс | Style injection |
| Frontend HTML | < 100 мс | DOM patching |

---

## 🎓 Дополнительно

### Мониторинг изменений

**Backend:**
```bash
# Смотрите директорию backend на изменения
find backend -type f -name "*.py" | entr -r python main.py
```

**Frontend:**
```bash
# React уже встроенный file watcher имеет
# Просто сохраняйте файлы, он обновится сам
```

### Отключение Hot Reload (если нужно)

**Backend:**
```bash
# Убрать --reload флаг
uvicorn main:app --host 0.0.0.0 --port 8888
```

**Frontend:**
```bash
# В package.json убрать FAST_REFRESH
FAST_REFRESH=false npm start
```

---

## 🎉 Готово!

Теперь вы можете разрабатывать с instant feedback без ручных перезагрузок! 🚀

**Любые вопросы?** Запустите `./start-dev.sh` и начните разрабатывать!
