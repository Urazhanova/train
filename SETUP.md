# 🚀 Инструкция по запуску

Пошаговое руководство для запуска Feedback Trainer локально.

## Шаг 1: Получить Groq API ключ

1. Перейдите на [console.groq.com](https://console.groq.com)
2. Зарегистрируйтесь или авторизуйтесь
3. Перейдите в Settings → API Keys
4. Создайте новый ключ и скопируйте его

## Шаг 2: Запустить Backend

### 2a. Создать виртуальное окружение

```bash
cd backend
python -m venv venv
```

На macOS/Linux:
```bash
source venv/bin/activate
```

На Windows:
```bash
venv\Scripts\activate
```

### 2b. Установить зависимости

```bash
pip install -r requirements.txt
```

### 2c. Конфигурировать окружение

```bash
cp .env.example .env
```

Отредактируйте `.env` и добавьте ваш Groq API ключ:
```
GROQ_API_KEY=gsk_xxxxxxxxxxxxx
```

### 2d. Запустить сервер

```bash
python main.py
```

или

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Вы должны увидеть:
```
INFO:     Uvicorn running on http://0.0.0.0:8000
```

Backend готов! Проверьте: `curl http://localhost:8000/health`

## Шаг 3: Запустить Frontend

### 3a. Установить зависимости

```bash
cd frontend
npm install
```

### 3b. Конфигурировать окружение

```bash
cp .env.example .env.local
```

`.env.local` должен содержать:
```
REACT_APP_API_URL=http://localhost:8000
```

### 3c. Запустить dev сервер

```bash
npm start
```

Браузер должен автоматически открыться на `http://localhost:3000`

## Шаг 4: Использовать приложение

1. **Начало диалога:** Приложение автоматически загружает первое сообщение менеджера
2. **Отправка сообщений:** Напишите ответ и нажмите Enter или кнопку "Отправить"
3. **Завершение:** Когда напишете 3+ сообщения, появится кнопка "Завершить и получить оценку"
4. **Результаты:** После завершения вы увидите оценку вашего общения

## 📋 Контрольный список

- [ ] Backend работает на `http://localhost:8000/health`
- [ ] Frontend работает на `http://localhost:3000`
- [ ] Groq API ключ установлен в `backend/.env`
- [ ] Диалог загружается корректно
- [ ] Сообщения отправляются и получают ответы
- [ ] Оценка выводится после завершения

## 🐛 Troubleshooting

### Backend не запускается

**Ошибка:** `ModuleNotFoundError: No module named 'fastapi'`

**Решение:**
```bash
pip install -r requirements.txt
```

**Ошибка:** `Groq API error`

**Решение:** Проверьте, что `GROQ_API_KEY` установлен в `.env`

### Frontend не загружает сообщения

**Ошибка:** "Сервер недоступен"

**Решение:**
1. Убедитесь, что backend запущен на `http://localhost:8000`
2. Проверьте, что `REACT_APP_API_URL` в `.env.local` правильный
3. Запустите `npm start` заново

### CORS ошибки

**Ошибка:** `Access to XMLHttpRequest blocked by CORS policy`

**Решение:** Backend уже имеет CORS настроен, но проверьте:
```python
# в main.py должно быть:
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    ...
)
```

## 📝 Завершение

Если все работает, то:

✅ Диалог отправляет сообщения
✅ Backend генерирует ответы через Groq
✅ Frontend показывает результаты оценки

**Готово! Тренажер готов к использованию.** 🎉

## 🔗 Полезные ссылки

- Backend API: http://localhost:8000
- Frontend: http://localhost:3000
- API документация: http://localhost:8000/docs (автоматическая Swagger документация)
- Groq Console: https://console.groq.com
