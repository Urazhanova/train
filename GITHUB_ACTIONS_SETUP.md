# 🚀 GitHub Actions Автоматический Деплой

Теперь при каждом `git push` на `main` ветку приложение автоматически деплоится!

## Что нужно сделать (5 минут)

### Шаг 1: Получить Railway Token

1. Открой https://railway.app
2. Лог ин через GitHub
3. Перейди в **Account** (нажми на аватар вверху справа) → **Tokens**
4. Нажми **Create New Token**
5. Назови его `GITHUB_ACTIONS` (например)
6. **СКОПИРУЙ ТОКЕН** (он больше не будет виден!)

### Шаг 2: Получить Railway Project ID

1. В Railway, создай новый проект (или выбери существующий):
   - **New Project** → **Deploy from GitHub**
   - Выбери `Urazhanova/train`
   - Railway сам найдет Procfile и создаст проект
2. Когда проект создан, скопируй его ID из URL:
   - `https://railway.app/project/[ТУТ_ID]`

### Шаг 3: Добавить Secrets в GitHub

1. Открой https://github.com/Urazhanova/train/settings/secrets/actions
2. Нажми **New repository secret**
3. Добавь эти 3 секрета:

   | Имя | Значение |
   |-----|---------|
   | `RAILWAY_TOKEN` | Токен из шага 1 |
   | `RAILWAY_PROJECT_ID` | ID из шага 2 |
   | `GROQ_API_KEY` | Твой Groq API ключ |

### Шаг 4: Создать Vercel Project (для фронтенда)

1. Открой https://vercel.com
2. **Add New** → **Project**
3. **Import Git Repository** → выбери `Urazhanova/train`
4. **Framework Preset**: React
5. **Root Directory**: `frontend`
6. **Environment Variables**:
   ```
   REACT_APP_API_URL = https://train-production-xxxx.railway.app
   ```
   (URL твоего бэкенда)
7. Нажми **Deploy**

### Шаг 5: Добавить Vercel Secrets (если нужен автодеплой с GitHub Actions)

1. Открой https://github.com/Urazhanova/train/settings/secrets/actions
2. Добавь эти секреты:
   - `VERCEL_TOKEN`: Получи на https://vercel.com/account/tokens
   - `VERCEL_ORG_ID`: Скопируй из URL при открытии проекта
   - `VERCEL_PROJECT_ID`: Найди в Team Settings → Project

---

## 🎯 Как это работает

**После добавления всех секретов:**

1. Ты делаешь `git push`
2. GitHub Actions автоматически:
   - Деплоит бэкенд на Railway
   - Деплоит фронтенд на Vercel
3. Через ~2-3 минуты app live! ✅

**Проверить статус**: https://github.com/Urazhanova/train/actions

---

## 📋 Альтернатива: Manual Deploy (без Actions)

Если хочешь всё делать вручную (не рекомендуется):

```bash
# Backend на Railway
railway login  # Откроется браузер
railway project select  # Выбери проект
railway up

# Frontend на Vercel
vercel --prod
```

---

## 🔄 Что делать при обновлениях

Просто комми и пуш:

```bash
git add .
git commit -m "Update something"
git push origin main
```

Всё остальное делает GitHub Actions! 🤖

---

## 🆘 Troubleshooting

**Actions не запускаются?**
- Проверь что Secrets добавлены правильно (Settings → Secrets)
- Посмотри логи: Actions таб в GitHub

**Бэкенд не деплоится?**
- Проверь RAILWAY_TOKEN правильный
- Проверь RAILWAY_PROJECT_ID совпадает
- Посмотри логи в Railway дашборде

**Фронтенд не деплоится?**
- Проверь VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID
- Убедись что REACT_APP_API_URL указывает на правильный бэкенд

---

## 📞 Готово!

После настройки у тебя будет:
- ✅ Автоматический деплой на каждый push
- ✅ Live URL для коллег
- ✅ Zero downtime обновления
- ✅ Профессиональный CI/CD pipeline

Поздравляем! 🎉
